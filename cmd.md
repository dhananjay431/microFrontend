# ng new mono --create-application false

#cd mono

#ng g application host-app
#ng g application mfe-app

cd ..

npm install --save-dev webpack webpack-cli

### auto

# ng generate @angular/elements:element shell --project=shell

# ng generate @angular/elements:element header --project=header

# ng generate @angular/elements:element footer --project=footer

# ng generate @angular/elements:element sidebar --project=sidebar

# ng generate @angular/elements:element dashboard --project=dashboard

# ng generate @angular/elements:element products --project=products

# ng generate @angular/elements:element cart --project=cart

# ng build shell --configuration production

# ng build header --configuration production

# ng build footer --configuration production

# ng build sidebar --configuration production

# ng build dashboard --configuration production

# ng build products --configuration production

# ng build cart --configuration production

# ng add @angular/pwa --project shell

# ng add @angular/pwa --project header

# ng add @angular/pwa --project footer

# ng add @angular/pwa --project sidebar

# ng add @angular/pwa --project dashboard

# ng add @angular/pwa --project products

# ng add @angular/pwa --project cart

# ng add @angular/pwa --project shell --configuration production

# ng add @angular/pwa --project header --configuration production

# ng add @angular/pwa --project footer --configuration production

# ng add @angular/pwa --project sidebar --configuration production

# ng add @angular/pwa --project dashboard --configuration production

# ng add @angular/pwa --project products --configuration production

# ng add @angular/pwa --project cart --configuration production
